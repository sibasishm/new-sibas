---
import { themeConfig } from '@/config'
import Head from '@/layouts/Head.astro'

import '@/styles/font.css'

const fontStyle = themeConfig.global.fontStyle === 'serif' ? 'font-serif' : 'font-sans'
const isPost = Astro.url.pathname.startsWith('/posts/')
---

<html
  lang={Astro.currentLocale}
  class:list={[
    fontStyle,
    { 'scroll-smooth': isPost },
  ]}
>
  <Head />
  <body>
    <div class="mx-auto max-w-4xl w-full px-4 sm:px-6">
      <div class="mt-8 flex items-center justify-between text-sm text-secondary font-medium lowercase">
        <nav class="flex items-start justify-between gap-4">
          <a href="/">sibas.in</a>
          <a href="/about">About</a>
          <a href="/thoughts">Thoughts</a>
        </nav>
        <div>Theme</div>
      </div>
      <main class="py-8 md:py-12">
        <slot />
      </main>
      <!-- <footer class="mt-10 text-center text-sm text-secondary">
        <p>
          &copy; {new Date().getFullYear()} Your Name. All rights reserved.
        </p>
        <p>
          Built with <a href="https://astro.build" class="text-secondary">Astro</a>.
        </p>
      </footer> -->
    </div>
  </body>
</html>
